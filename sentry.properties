# Sentry configuration for NumVault
# Used by sentry-cli and EAS/native builds for source-map and debug-symbol uploads.
# This file contains NO secrets. Auth token must be set as SENTRY_AUTH_TOKEN env var.
# See docs/sentry.md for full upload instructions.

defaults.url=https://sentry.io/
defaults.org=supremeanalytics
defaults.project=numvault
